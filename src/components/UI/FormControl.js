import { FormControl as FormControlMaterial } from "@mui/material";
import styled from "styled-components";

const FormControl = styled(FormControlMaterial)`
  width: 100%;
`;

export default FormControl;
